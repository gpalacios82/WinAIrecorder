<Window x:Class="VoiceType.ConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Win AI Recorder — Settings"
        Width="360" Height="368"
        WindowStyle="SingleBorderWindow"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource TextBrush}"
        ShowInTaskbar="False">

    <Window.Resources>
        <ControlTemplate x:Key="FlatBtn" TargetType="Button">
            <Border x:Name="bd"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="3"
                    Padding="{TemplateBinding Padding}">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="bd" Property="Opacity" Value="0.82"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter TargetName="bd" Property="Opacity" Value="0.65"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="7,0"/>
            <Setter Property="CaretBrush" Value="{DynamicResource TextBrush}"/>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="7,0"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="28"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="Label">
            <Setter Property="Foreground" Value="{DynamicResource SubtextBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Width" Value="68"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20,16,20,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Title -->
            <RowDefinition Height="14"/>
            <RowDefinition Height="28"/>    <!-- API Key -->
            <RowDefinition Height="7"/>
            <RowDefinition Height="28"/>    <!-- Model -->
            <RowDefinition Height="7"/>
            <RowDefinition Height="28"/>    <!-- Hotkey -->
            <RowDefinition Height="7"/>
            <RowDefinition Height="28"/>    <!-- Theme -->
            <RowDefinition Height="14"/>
            <RowDefinition Height="1"/>     <!-- Separator -->
            <RowDefinition Height="14"/>
            <RowDefinition Height="Auto"/>  <!-- Checkboxes -->
            <RowDefinition Height="*"/>
            <RowDefinition Height="28"/>    <!-- Buttons -->
        </Grid.RowDefinitions>

        <!-- Title + current key hint -->
        <DockPanel Grid.Row="0">
            <TextBlock DockPanel.Dock="Left" Text="Win AI Recorder"
                       FontSize="13" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextBrush}"/>
            <TextBlock x:Name="CurrentKeyText" DockPanel.Dock="Right"
                       HorizontalAlignment="Right"
                       Text="" FontSize="11"
                       Foreground="{DynamicResource SubtextBrush}"
                       VerticalAlignment="Center"/>
        </DockPanel>

        <!-- API Key -->
        <DockPanel Grid.Row="2">
            <TextBlock DockPanel.Dock="Left" Text="API Key" Style="{StaticResource Label}"/>
            <Button DockPanel.Dock="Right"
                    x:Name="ValidateKeyBtn"
                    Content="✓ Validar" Width="72"
                    Template="{StaticResource FlatBtn}"
                    Background="{DynamicResource SurfaceBrush}"
                    Foreground="{DynamicResource SubtextBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1" FontSize="11"
                    Click="ValidateKey_Click"
                    Margin="6,0,0,0"/>
            <PasswordBox x:Name="ApiKeyBox"
                         PasswordChanged="ApiKeyBox_PasswordChanged"/>
        </DockPanel>

        <!-- Model -->
        <DockPanel Grid.Row="4">
            <TextBlock DockPanel.Dock="Left" Text="Model" Style="{StaticResource Label}"/>
            <Button DockPanel.Dock="Right"
                    Content="↺" Width="28"
                    Template="{StaticResource FlatBtn}"
                    Background="{DynamicResource SurfaceBrush}"
                    Foreground="{DynamicResource TextBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1" FontSize="13"
                    Click="RefreshModels_Click"
                    Margin="6,0,0,0"/>
            <TextBlock x:Name="ModelLoadingText"
                       Text="cargando…" FontSize="11"
                       Foreground="{DynamicResource SubtextBrush}"
                       VerticalAlignment="Center"
                       Visibility="Collapsed"/>
            <ComboBox x:Name="ModelComboBox"/>
        </DockPanel>

        <!-- Hotkey -->
        <DockPanel Grid.Row="6">
            <TextBlock DockPanel.Dock="Left" Text="Hotkey" Style="{StaticResource Label}"/>
            <Button DockPanel.Dock="Right"
                    Content="↺" Width="28"
                    Template="{StaticResource FlatBtn}"
                    Background="{DynamicResource SurfaceBrush}"
                    Foreground="{DynamicResource TextBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1" FontSize="13"
                    Click="ResetHotkey_Click"
                    Margin="6,0,0,0"/>
            <TextBox x:Name="HotkeyBox"
                     IsReadOnly="True"
                     PreviewKeyDown="HotkeyBox_PreviewKeyDown"
                     GotFocus="HotkeyBox_GotFocus"
                     LostFocus="HotkeyBox_LostFocus"/>
        </DockPanel>

        <!-- Theme -->
        <DockPanel Grid.Row="8">
            <TextBlock DockPanel.Dock="Left" Text="Tema" Style="{StaticResource Label}"/>
            <ComboBox x:Name="ThemeComboBox" SelectionChanged="ThemeComboBox_SelectionChanged">
                <ComboBoxItem Tag="auto">Auto (sistema)</ComboBoxItem>
                <ComboBoxItem Tag="dark">Oscuro</ComboBoxItem>
                <ComboBoxItem Tag="light">Claro</ComboBoxItem>
            </ComboBox>
        </DockPanel>

        <!-- Separator -->
        <Border Grid.Row="10" Height="1" Background="{DynamicResource BorderBrush}"/>

        <!-- Options -->
        <StackPanel Grid.Row="12">
            <CheckBox x:Name="ClipboardFallbackCheck"
                      Content="Copiar al portapapeles como fallback"/>
            <CheckBox x:Name="AutoStartCheck"
                      Content="Iniciar con Windows"
                      Margin="0,8,0,0"/>
        </StackPanel>

        <!-- Buttons -->
        <Grid Grid.Row="14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="StatusText" Grid.Column="0"
                       VerticalAlignment="Center" FontSize="11"
                       Foreground="{DynamicResource SubtextBrush}"/>

            <Button Grid.Column="1" Content="Cancelar" Width="78"
                    Template="{StaticResource FlatBtn}"
                    Background="{DynamicResource SurfaceBrush}"
                    Foreground="{DynamicResource TextBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    Click="Cancel_Click"/>

            <Button Grid.Column="3" Content="Guardar" Width="78"
                    Template="{StaticResource FlatBtn}"
                    Background="{DynamicResource AccentBrush}"
                    Foreground="White"
                    BorderThickness="0"
                    Click="Save_Click"/>
        </Grid>
    </Grid>
</Window>
